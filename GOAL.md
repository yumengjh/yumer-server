# 发布态出口渲染与块级 HTML 懒缓存设计文档

## 背景

当前后端已经具备文档内容存储能力，文档内容由多个块组成，块内容通常以结构化 JSON 形式存储。

为了提升发布态、阅读态、SSR 页面或公开文档访问时的渲染性能，需要在后端增加一套统一的渲染出口能力。

本方案的目标不是重写编辑器，也不是改变编辑态数据模型，而是在内容返回出口增加一个统一的渲染层，并在渲染过程中支持块级 HTML 缓存。

## 目标

实现一个后端渲染出口，使发布态文档在返回前可以被转换为阅读态 HTML。

渲染过程中，如果某个块已经存在可用的 HTML 缓存，则直接复用缓存结果；如果不存在缓存，则现场渲染该块，并在合适时机将渲染结果回填到该块对应的版本数据中。

整体目标：

- 提供统一的文档渲染入口
- 避免多个接口、多个模块各自实现渲染逻辑
- 支持整篇文档渲染
- 支持块级 HTML 缓存复用
- 支持首次访问时自动生成缓存
- 支持部分块类型不参与缓存
- 保持编辑态数据和发布态渲染产物的边界清晰
- 为后续 SSR、公开文档、导出、SEO、CDN 缓存打基础

## 非目标

本次不要求实现复杂的分布式缓存。

本次不要求引入 Redis、消息队列、独立构建服务或 CDN 控制逻辑。

本次不要求实现完整的增量发布系统。

本次不要求重构现有文档、块、版本的核心存储模型。

本次不要求处理复杂协同编辑同步问题。

本次重点是建立一个稳定、可扩展、可复用的后端渲染出口。

## 核心概念

### 编辑态内容

编辑态内容是系统中的原始内容数据，通常为块结构或 Tiptap JSON。

编辑态内容是事实来源，不应该被 HTML 缓存替代。

### 发布态渲染结果

发布态渲染结果是由编辑态内容经过后端渲染后生成的 HTML。

它是派生产物，可以被缓存、失效、重新生成。

### 块级 HTML 缓存

块级 HTML 缓存是某个块版本在指定渲染规则下生成的 HTML 结果。

缓存应该绑定到块版本，而不是仅绑定到块本身。

原因是：同一个块在内容更新后会产生新的版本，旧版本的 HTML 不应该污染新版本内容。

### 渲染版本

渲染结果需要具备渲染版本概念。

当渲染规则、内容 schema、HTML 输出格式、安全过滤规则等发生变化时，旧缓存应视为不可用，并重新生成。

## 功能设计

### 统一渲染入口

后端需要提供统一的渲染服务，用于处理文档或块的发布态渲染。

该服务应该作为后端内部能力存在，供公开文档接口、SSR 接口、发布接口、导出接口等复用。

渲染入口至少应支持：

- 渲染整篇文档
- 渲染单个块
- 判断块级 HTML 缓存是否可用
- 在缓存不可用时现场渲染
- 在渲染后回填块级 HTML 缓存

### 文档渲染流程

当请求发布态文档时，后端应按以下逻辑处理：

```txt
读取文档发布态数据
↓
读取文档包含的块版本
↓
逐个处理块
↓
优先使用有效的块级 HTML 缓存
↓
缓存不存在或已失效时，现场渲染该块
↓
组合所有块 HTML
↓
返回完整阅读态内容
↓
必要时回填新生成的块级 HTML 缓存
```

### 块级缓存读取

每个块在渲染前需要判断是否存在可复用的 HTML 缓存。

判断条件包括但不限于：

- 当前块版本是否已有 HTML 缓存
- 当前缓存是否由当前渲染版本生成
- 当前块类型是否允许使用缓存
- 当前块内容是否适合缓存

如果缓存可用，则直接使用缓存。

如果缓存不可用，则进入现场渲染流程。

### 块级缓存回填

当某个块没有可用 HTML 缓存时，渲染服务应现场生成 HTML。

生成后的 HTML 可以写回对应的块版本数据中，作为后续请求的缓存。

缓存回填不应该破坏主流程。

如果缓存写入失败，不应影响本次文档正常返回。

### 不参与缓存的块

系统需要允许某些块不参与 HTML 缓存。

例如：

- 动态内容块
- 外部嵌入块
- 依赖实时数据的块
- 未来的 AI 生成块
- 需要每次重新计算的特殊块

这些块可以每次现场渲染，或者返回占位结构，由前端进一步处理。

### 渲染结果组合

整篇文档的最终 HTML 应由多个块级 HTML 结果组合而成。

组合逻辑应保持稳定，避免在不同接口中重复实现。

文档级渲染结果可以在未来进一步缓存，但本次重点是块级 HTML 缓存。

### 缓存失效

....

### 错误处理

渲染过程中，如果某个块渲染失败，不应导致整篇文档完全不可用。

系统应提供合理降级策略，例如：

- 返回该块的安全占位内容
- 跳过该块
- 返回错误提示块
- 记录渲染错误日志

具体降级方式由后端现有风格决定。

但必须保证错误可追踪，不允许静默吞掉关键异常。

### 安全要求

后端渲染出的 HTML 必须经过安全处理，避免产生明显的 XSS 风险。

渲染服务需要明确区分：

- 原始内容
- 渲染产物
- 可安全返回给用户的 HTML

如果现有项目已经有 HTML 安全处理逻辑，应优先复用。

如果没有，应在渲染出口统一处理，而不是让多个接口分别处理。

## 接口行为

### 发布态文档接口

发布态文档接口应返回适合阅读态使用的内容。

返回结果可以包含：

- 文档基础信息
- 文档结构信息
- 渲染后的 HTML 内容
- 必要的块元信息

不要求暴露完整编辑态 JSON，除非当前业务确实需要。

### 内部渲染能力

渲染服务应作为后端内部模块存在。

其他模块不应该直接操作块 HTML 缓存，而应该通过统一渲染服务完成。

## 数据要求

需要为块版本保存渲染产物相关信息。

至少需要表达以下信息：

- 渲染后的 HTML
- 渲染版本
- 渲染时间
- 渲染状态或错误信息，视项目需要决定

具体字段命名、数据库迁移方式、实体设计由 Agent 根据当前后端项目结构决定。

不要为了本功能重构整个数据模型。

## 日志要求

渲染服务需要记录关键日志，方便排查问题。

至少应覆盖：

- 文档渲染开始和结束
- 块级缓存命中
- 块级缓存未命中
- 块级渲染失败
- 缓存回填失败
- 渲染版本不匹配导致的重新渲染

日志不需要过度详细，但必须能定位问题。

## 性能要求

首次访问允许发生全量渲染。

后续访问应尽量复用块级 HTML 缓存，避免重复渲染所有块。

缓存回填不应明显拖慢主请求。

大文档渲染时应避免不必要的重复数据库写入。

如果多个块没有缓存，可以批量处理缓存写入。

## 扩展性要求

本功能应为后续能力预留空间：

- 文档级 HTML 缓存
- SSR 页面缓存
- Markdown 导出
- PDF 导出
- CDN 缓存
- 增量发布
- 块级懒加载
- 动态块渲染
- AI 块生成
- 多渲染主题

但本次不要一次性实现这些能力。

当前只需要把渲染出口和块级 HTML 懒缓存的基础能力做好。

## 开发要求

Agent 需要在现有后端项目中实现该能力。

实现前应先阅读当前后端项目结构，识别已有的：

- 文档模块
- 块模块
- 版本模块
- 发布接口
- 数据库实体
- 服务层结构
- 错误处理方式
- 日志方式

实现时应尽量贴合现有项目风格，不要引入过重的新架构。

不要擅自引入 Redis、消息队列、独立 worker 或大型缓存框架。

如确实需要异步回填，可以先使用项目内部已有机制，或采用简单、可维护的方式完成。

## 验收标准

完成后应满足以下结果：

- 后端存在统一的文档渲染出口
- 发布态文档可以返回渲染后的 HTML
- 块没有 HTML 缓存时可以现场渲染
- 块渲染后可以回填 HTML 缓存
- 下次访问相同块版本时可以复用 HTML 缓存
- 渲染版本不匹配时可以重新渲染
- 可以配置或判断部分块不参与缓存
- 单个块渲染失败不会导致整篇文档不可用
- 关键渲染过程有日志
- 不破坏现有编辑态数据结构
- 不影响现有文档编辑、保存、读取流程

## 总结

本方案的核心不是单纯缓存 HTML，而是在后端建立一个统一的发布态渲染出口。

块级 HTML 缓存只是该出口内部的性能优化手段。

最终目标是让后端具备稳定的内容发布能力，使文档系统从“编辑器数据存储”逐步升级为“内容发布基础设施”。
